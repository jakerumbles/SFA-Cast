Screenshot help:

To take a screenshot press F12 on your keyboard.

It will create a folder called "SFACAST-Screenshots" in the SFA-CAST folder the first time that it is used.
Then it will continue save them in that folder as long as the name is not changed.
The program defaults to that folder that is already created, unless:
-the name is changed
-there is more than one folder with that name